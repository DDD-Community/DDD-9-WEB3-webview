import { ButtonStyledProps } from '@components/button/model';
import styled from 'styled-components';

export const LoginStyled = styled.section`
  --button-width: 45px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 20px 0;
`;

export const LoginSignupAreaStyled = styled.article`
  padding: 10px 0;
`;
